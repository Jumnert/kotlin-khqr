# JitPack build configuration.
# JitPack builds the library on demand when someone requests
#   com.github.<user>:kotlin-bakong:<tag>
# Gradle 9.6.1 (via the wrapper) and Kotlin 2.4.0 run on JDK 17.
jdk:
  - openjdk17
install:
  - ./gradlew clean publishToMavenLocal -x test --no-daemon
